import React from "react";

const NotFound = () => {
  return (
    <div
      className="display-4"
      style={{
        top: 0,
        display: "flex",
        alignItems: "center",
        justifyContent: "center",
        textAlign: "center",
      }}
    >
      Error 404
    </div>
  );
};

export default NotFound;
